[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "search_engine"
description = "Search Engine"
dynamic = ["dependencies"]
version = "0.0.1"

[tool.setuptools]
package-dir = {"" = "src"}

[project.scripts]
search_engine = "search_engine.main:run"

