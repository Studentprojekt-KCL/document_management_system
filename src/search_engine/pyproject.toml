[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "se_api"
description = "Search Engine"
dynamic = ["dependencies"]
version = "0.0.1"

[tool.setuptools]
package-dir = {"" = "src"}

[project.scripts]
se_api = "se_api.api:run"

