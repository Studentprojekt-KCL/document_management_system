[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "smb_connector_service"
description = ""
version = "0.1.0"
dynamic = ["dependencies"]

[tool.setuptools]
package-dir = {"" = "src"}

[project.scripts]
smb_connector = "smb_api:run"
