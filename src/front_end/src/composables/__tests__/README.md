# Composables Tests

## useSearchMetadata.js — Pure logic

Tests for standalone resolver functions (`resolveFilename`, `resolveDocumentType`, `resolveDocumentExtension`, `resolveSource`, `resolveDateOnly`, `resolveLink`, `resolveSecurityClass`) and the `useSearchMetadata` composable computed properties + `normalizeMatches`.
