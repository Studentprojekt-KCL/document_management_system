[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gitlab_service"
description = ""
version = "0.1.0"
dependencies = [
    "requests>=2.32",
    "dmis_shared_functions @ file:../../shared_functions",
]

[tool.setuptools]
package-dir = {"" = "src"}
