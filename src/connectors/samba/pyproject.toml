[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "samba_service"
description = ""
version = "0.1.0"
dynamic = ["dependencies"]

[tool.setuptools]
package-dir = {"" = "src"}

[project.scripts]
samba_connector = "api:run"
